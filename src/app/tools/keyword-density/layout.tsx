import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Keyword Density Checker — Free ASO Tool | ASOHack",
  description:
    "Analyze keyword density, character counts, and missing keyword opportunities in your App Store or Google Play listing. Free, no login required.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
